{
  "name": "espace",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "dev:all": "concurrently \"npm --prefix backend run start:dev\" \"npm --prefix frontend run dev\"",
    "build": "npm --prefix backend run build && npm --prefix frontend run build",
    "seed": "npm --prefix backend run prisma:seed",
    "db:reset": "npm --prefix backend exec prisma migrate reset",
    "db:up": "docker compose up -d",
    "db:down": "docker compose down",
    "db:logs": "docker compose logs -f",
    "lint": "npm --prefix backend run lint:check && npm --prefix frontend run lint",
    "check": "npm --prefix backend run build && npm --prefix frontend run build"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "lucide-react": "^0.575.0",
    "next": "14.1.0",
    "next-intl": "^3.28.1",
    "tailwindcss": "^3.4.17",
    "pdfkit": "^0.18.0",
    "xlsx": "^0.18.5"
  }
}
